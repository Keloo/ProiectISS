<header id="header">
    <div id="logo-group">
        <span id="logo" style="margin-top: 10px">
            <img style="width: auto; height: 50px;" src="{{ asset('bundles/app/img/panda.png') }}" alt="Panda">
        </span>
    </div>

    <span id="extr-page-header-space">
            <span class="hidden-mobile">Need an account?</span>
            <a href="{{ path('fos_user_registration_register') }}" class="btn btn-danger">Create account</a>
        </span>
</header>
